#include <stdlib.h>
#include "types.h"

/*
creates a hash table
void *array is a pointer the said array
char **map is the strings which map to the indexes of the array
*/
hash_t new_table(void *array, char **map)
{
	hash_t new;
	unsigned size;

	s
}
